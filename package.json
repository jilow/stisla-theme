{
  "name": "stisla-theme",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "files": "./dist",
  "scripts": {
    "copy-src": "cpy ./scss/**/*.scss ../../src --parents --cwd ./stisla/sources",
    "copy-dist": "cpy ./**/*.scss ../../dist/scss --parents --cwd ./src/scss",
    "compile": "node-sass ./src/scss -o ./dist/css",
    "prepublish": "npm run compile && npm run copy-dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "node-sass": "^4.11.0",
    "cpy-cli": "^2.0.0"
  }
}
